<project_context>
project_name: Demo
description: A demo project.
folder: <TEMPDIR>
build_command: cargo build
test_command: cargo test
platform: <PLATFORM_INFO>
</project_context>

<dev_loop_workflow>
Edit code with apply_patch. Finish with task_done.

apply_patch envelope (atomic - any directive failure rejects the whole patch):

*** Begin Patch
*** Add File: path/to/new.rs
+content line
*** Update File: path/to/existing.rs
@@ optional context header
 unchanged context
-removed line
+added line
*** Delete File: path/to/old.rs
*** End Patch

Paths workspace-relative, forward-slash, no `./` or `..`. Update hunks need exact context - read_file first to derive it.

Invariants:
- Read a file before editing it.
- task_done only when `cargo build` and `cargo test` are both green; the harness re-runs `cargo test` as a hard gate. If no changes are needed, call task_done with `no_changes_needed: true`.
- Never run: git push --force, git reset --hard, git clean -fd, git config. Do not touch .gitignore to hide build output.
</dev_loop_workflow>