//! # aura-agent-steering
//!
//! Placeholder for the steering evaluators (`RepeatedReadTracker`,
//! `TurnSteering`, `SteeringRegistry`, the `EarlyOracle` /
//! `RepeatedRead` / `ImplementNow` / `Inject` evaluator family).
//! Phase 6a populates this crate by extracting
//! `crates/aura-agent/src/agent_loop/steering/` into here.
//!
//! Phase 3 ships an empty crate so the workspace layout matches the
//! target layered shape; no public items are exported yet.
//!
//! Layer: agent

#![forbid(unsafe_code)]
#![warn(clippy::all)]
