[package]
name = "aura-fleet-subagent"
description = "Fleet-layer concrete SubagentDispatchHook impl wrapping FleetSpawner"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
aura-core-types = { path = "../aura-core-types" }
aura-store-db = { path = "../aura-store-db" }
aura-tools = { path = "../aura-tools" }
aura-agent-subagent = { path = "../aura-agent-subagent" }
aura-fleet-spawn = { path = "../aura-fleet-spawn" }
aura-fleet-registry = { path = "../aura-fleet-registry" }
aura-fleet-quota = { path = "../aura-fleet-quota" }
# `AgentLoopEvent` type for the optional child streaming sink threaded
# through `spawn_with_events` (no agent-loop logic runs in this crate).
aura-agent-loop = { path = "../aura-agent-loop" }
async-trait = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
tokio-util = { workspace = true }

[dev-dependencies]
aura-engine = { path = "../aura-engine" }
aura-store-db = { path = "../aura-store-db" }
aura-model-reasoner = { path = "../aura-model-reasoner" }
aura-core-modes = { path = "../aura-core-modes" }
aura-core-permissions = { path = "../aura-core-permissions" }
tempfile = { workspace = true }
insta = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
